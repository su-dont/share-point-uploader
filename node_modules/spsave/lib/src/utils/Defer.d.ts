export interface IDeferred<T> {
    resolve: (result: T) => void;
    reject: (err: T) => void;
    promise: Promise<T>;
    isPending(): boolean;
}
export declare function defer<T>(): IDeferred<T>;
