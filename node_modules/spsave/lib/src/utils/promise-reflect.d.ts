import { HTTPError } from 'sp-request';
export declare function reflect<T>(promise: Promise<T>): Promise<{
    isRejected: boolean;
    isResolved: boolean;
    reason: HTTPError;
    data: T;
}>;
